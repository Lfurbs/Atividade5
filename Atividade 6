#include <stdio.h>

main()
{

    int num;

    printf("Digite um numero:");
    scanf("%d", &num);

    if (numero % 2 == 0)
    {
        printf("O numero digitado é par");
    }
    else
    {   printf("O numero digitado é impar");
    }
}
